box: invalidname/cpp11@0.0.3

build:
    steps:
        -script:
            name: install deps
            code: |
                sudo apt-get install -y libyaml-dev check
        - script:
            name: make
            code: |
                make package
                target/kanabo --version
